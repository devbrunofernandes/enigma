#include "interactiveUI.hpp"
#include "argumentUI.hpp"

int main(int argc, char* argv[]) {
    if(argc == 1) {
        interactiveMode();
    } else {
        // argumentMode();
    }

    return 0;
}